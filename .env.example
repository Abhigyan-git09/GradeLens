# Generate a long random value before starting the production stack.
GRADELENS_WRITE_API_KEY=replace-with-at-least-32-random-characters

# Host port exposed by the frontend reverse proxy.
GRADELENS_PORT=8080

# Optional. Deterministic grounded explanations work without this value.
GRADELENS_LLM_API_KEY=
